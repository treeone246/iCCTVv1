# ============================================================================
# Jetson Orin Nano Super profile (JetPack 6.2 / L4T 36.4.3 / Ubuntu 22.04)
# CUDA 12.6 / cuDNN 9.3 / TensorRT 10.3 / Python 3.10 (aarch64)
# ============================================================================
#
# This file is intentionally tailored for Jetson and NOT for x86 laptops.
#
# Pre-reqs from NVIDIA docs (install once on device):
#   sudo apt-get update
#   sudo apt-get install -y python3-pip python3-opencv libopenblas-dev libopenmpi-dev libomp-dev
#
# PyTorch on Jetson must be installed from Jetson-compatible wheels:
# NVIDIA docs:
#   https://docs.nvidia.com/deeplearning/frameworks/install-pytorch-jetson-platform/index.html
# Ultralytics Jetson guide (JP6, Python 3.10):
#   https://docs.ultralytics.com/guides/nvidia-jetson/
#
# --- Jetson-specific binary wheels (Python 3.10, aarch64) ---
https://github.com/ultralytics/assets/releases/download/v0.0.0/torch-2.10.0-cp310-cp310-linux_aarch64.whl ; platform_system == "Linux" and platform_machine == "aarch64" and python_version == "3.10"
https://github.com/ultralytics/assets/releases/download/v0.0.0/torchvision-0.25.0-cp310-cp310-linux_aarch64.whl ; platform_system == "Linux" and platform_machine == "aarch64" and python_version == "3.10"
https://github.com/ultralytics/assets/releases/download/v0.0.0/onnxruntime_gpu-1.23.0-cp310-cp310-linux_aarch64.whl ; platform_system == "Linux" and platform_machine == "aarch64" and python_version == "3.10"

# --- Runtime packages for ppe_monitor ---
numpy==1.26.4
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.10.6
PyYAML==6.0.2
websockets==14.2
ultralytics==8.4.48
lap>=0.5.12

# Test dependencies (optional on target device)
pytest==8.3.4
