--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cu126
--prefer-binary

# Core numeric stack
numpy==1.26.4

# CUDA-enabled PyTorch for Linux x86_64
torch==2.6.0 ; platform_system == "Linux" and platform_machine == "x86_64" and python_version == "3.10"
torchvision==0.21.0 ; platform_system == "Linux" and platform_machine == "x86_64" and python_version == "3.10"

# App runtime
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.10.6
PyYAML==6.0.2
websockets==14.2
ultralytics==8.4.48
lap>=0.5.12
prometheus-client==0.21.1

# OpenCV runtime (headless for containers)
opencv-python-headless==4.10.0.84

# ONNX Runtime CUDA provider
onnxruntime-gpu==1.23.0 ; platform_system == "Linux" and platform_machine == "x86_64" and python_version == "3.10"

# Optional test dependency used by CI
pytest==8.3.4
